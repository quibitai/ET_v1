// TEMPORARILY DISABLED TO RESOLVE CIRCULAR DEPENDENCY ISSUES
// TODO: Re-enable relations after schema migration is complete

/*
import { relations } from 'drizzle-orm';
import {
  clients,
  user,
  chat,
  message,
  vote,
  conversationEntities,
  conversationSummaries,
  chatFileReferences,
  conversationalMemory,
  specialists,
  analyticsEvents,
  mcpServers,
  userMcpIntegrations,
} from './schema';

// All relations temporarily commented out for schema migration
*/

export {}; // Keep file as valid module
